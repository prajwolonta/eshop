import React from 'react'


const Category = (props)=>{
    // console.log(props)
    return (
        <div>
            {props.children}
            <ul>

            </ul>
        </div>
    )
}

export default Category